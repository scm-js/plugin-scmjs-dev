{
  "name": "@scm-js/plugin-scmjs-dev",
  "version": "1.13.0",
  "private": true,
  "description": "scmjs.dev — a plugin for the scmJS StarCraft map editor: your account, map storage with revisions, and the AI features (scenarios, maps, triggers, an assistant) on one free trial and one sign-in.",
  "author": "Jeany",
  "license": "MIT",
  "type": "module",
  "types": "./contract.d.ts",
  "exports": {
    ".": {
      "types": "./contract.d.ts"
    },
    "./contract": {
      "types": "./contract.d.ts"
    },
    "./protocol": {
      "types": "./protocol.ts"
    }
  },
  "scripts": {
    "build": "esbuild plugin.ts --bundle --format=esm --target=es2022 --platform=browser --outfile=dist/plugin.js --banner:js=\"// Generated by npm run build - do not edit; the source is plugin.ts.\"",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@scm-js/plugin-api": "^1.16.0",
    "esbuild": "^0.28.2",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
